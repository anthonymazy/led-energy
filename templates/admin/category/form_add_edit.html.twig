{% extends 'admin/base.html.twig' %}

{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block body %}
    <div class="container">
        <h3 class="mt-5">{{ message }} une catégorie</h3>

        {{ form_start(form) }}

        {{ form_widget(form.name) }}

        <button type="submit" class="btn btn-primary mt-3">{{ message }} la catégorie</button>

        {{ form_end(form) }}
    </div>
{% endblock %}